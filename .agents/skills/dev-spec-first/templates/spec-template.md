# 开发规格模板（供 dev-spec-first Skill 内部引用）

## 开发 Prompt

### 任务描述
[一句话说清楚这次要做什么，不超过2行]

### 文件路径
- 目标文件：`[路径]`
- 参考文件：`[路径，如参考 HTML / 设计稿，如有]`
- 接口线索：`[接口名/前缀，如有]`

### 改动范围
只允许修改：
1. `[文件名]` 中 `[具体区域/节点/方法名]`
2. `[文件名]` 中 `[对应样式块]`
3. 如必须改 script，只改 `[最小范围]`

### 禁改区域
不允许修改：
1. header 顶部区域
2. [其他明确禁改的组件/区域]
3. 接口请求逻辑
4. 字段名 / 数据结构
5. 现有业务方法的内部逻辑

### 复用要求
必须复用：
1. 现有 `[方法名]` 方法，不重写
2. 现有 `[数据字段]` 数据绑定
3. 现有 `[组件]` 组件，不引入新框架

### 实现约束
- 当前框架：[Vue2 / React / 原生 JS / ...]
- 禁用语法：[如 Vue2 禁用 ?. ?? <script setup>]
- 组件库：[如 ElementUI，已注册组件列表见 elementui.js]
- 不允许整文件重写，必须精确替换目标节点

### 执行方式
> 先读取真实文件 → 定位目标节点 → 只改标注区域 → 输出改动清单

请按以下顺序输出：
1. 识别到的当前节点（文件名 + 行号范围）
2. 需要替换的 template 片段
3. 需要替换/新增的样式片段
4. 如必须改 script，只给最小改动 + 说明原因
5. 明确列出"本次没有改动的区域"

### 验收标准
- ✅ [条件1]
- ✅ [条件2]
- ✅ [条件3]
- ❌ 非目标区域不应有任何变动
- ❌ 不应重写整个文件
- ❌ 不应改动业务逻辑/接口调用
