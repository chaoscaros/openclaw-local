# 开发规格模板（供 dev-spec-first Skill 内部引用）

## 开发 Prompt

### 任务描述

[一句话说清楚这次要做什么，不超过2行]

### 文件路径

- 目标文件：`[路径]`
- 参考文件：`[路径，如参考 HTML / 设计稿，如有]`
- 接口线索：`[接口名/前缀，如有]`

### 改动范围

只允许修改：

1. `[文件名]` 中 `[具体区域/节点/方法名]`
2. `[文件名]` 中 `[对应样式块]`
3. 如必须改 script，只改 `[最小范围]`

### 禁改区域

不允许修改：

1. `[明确不能动的组件/区域]`
2. `[明确不能动的接口/数据结构/业务逻辑]`
3. `[其他边界]`

### 复用要求

必须复用：

1. 现有 `[方法名]` 方法，不重写
2. 现有 `[数据字段]` 数据绑定
3. 现有 `[组件]` 组件，不引入新框架

### 实现约束

- 当前框架：[Vue2 / React / 原生 JS / ...]
- 禁用语法：[如 Vue2 禁用 ?. ?? <script setup>]
- 组件库：[如 ElementUI，已注册组件列表见 elementui.js]
- 不允许整文件重写，必须精确替换目标节点

### 输出要求

- 先读取真实文件，再开始改动
- 只做最小改动，不扩散到非目标区域
- 改完后输出：改了什么 / 没改什么
- 如果这是给 OpenClaw / Codex / Cursor 的执行单，内容要可直接转发，无需二次补充

### 验收标准

- ✅ [条件1]
- ✅ [条件2]
- ✅ [条件3]
- ❌ 非目标区域不应有任何变动
- ❌ 不应重写整个文件
- ❌ 不应改动业务逻辑/接口调用
